import React, { useState } from "react";
import PropTypes from 'prop-types';
import styles from './item-cart.module.scss';

function ItemCart (props) {
    const {
        image, 
        attributes
    } = props;
    return (
        <div>Add Item Will be here</div>
    )
}

ItemCart.propTypes = {

}

export default ItemCart;